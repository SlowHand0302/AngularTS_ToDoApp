export enum TodoSkeletonVariants {
  TODO_ITEM = 'todo-item',
  TODO_FORM_EDIT = 'todo-form__edit',
  TODO_DETAILS = 'todo-details',
}

export enum TaskStatus {
  TODO = 'TODO',
  IN_PROGRESS = 'IN_PROGRESS',
  DONE = 'DONE',
  BACKLOG = 'BACKLOG',
  CANCELED = 'CANCELED',
}

export enum TaskType {
  DOC = 'DOC',
  FIX = 'FIX',
  FEATURE = 'FEATURE',
  TEST = 'TEST',
  REFACTOR = 'REFACTOR',
}

export enum TaskPriority {
  HIGH = 'HIGH',
  MEDIUM = 'MEDIUM',
  LOW = 'LOW',
}
